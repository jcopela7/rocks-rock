{
  "name": "jonrocks-monorepo",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format:tsx": "prettier --write .",
    "format:tsx:check": "prettier --check .",
    "format:swift": "swift-format -r . -i"
  }
}
