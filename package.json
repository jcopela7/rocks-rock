{
  "name": "jonrocks-monorepo",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
